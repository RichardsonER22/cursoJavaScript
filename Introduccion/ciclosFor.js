const nombres=['caperry','maistro','banimaru','tozi','berijon','donlalo','truku'];
//nombres.forEach((nombres)=>{
 //   console.log(nombres);

//});

/*Ciclo for
repite un bloque de codigo mientras se cumpla la condicion

expresion 1: se ejecuta una sola vez antes de comenzar a repetir un bloque de codigo
expresion 2: una condicion, mientras se cumpla se ejecutara en el bloque de codigo
expresion 3: esta expresion se ejecuta siempre y despues de que se ejecute el bloque de codigo
*/

for(let numero =0; numero< nombres.length; numero++ ){
    console.log(nombres[numero])
}
